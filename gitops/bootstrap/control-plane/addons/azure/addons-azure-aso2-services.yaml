# ---
# apiVersion: argoproj.io/v1alpha1
# kind: ApplicationSet
# metadata:
#   name: addons-azure-service-bus
# spec:
#   syncPolicy:
#     preserveResourcesOnDeletion: true
#   generators:
#     - merge:
#         mergeKeys: [server]
#         generators:
#           - clusters:
#               values:
#                 addonChart: azure-service-bus
#                 addonChartVersion: '1.0.0'
#                 addonChartRepository: https://marketplace.azurecr.io/helm/v1/repo
#               selector:
#                 matchExpressions:
#                   - key: environment
#                     operator: NotIn
#                     values: ['control-plane']
#                   - key: enable_azure_service_bus
#                     operator: In
#                     values: ['true']
#   template:
#     metadata:
#       name: addon-{{name}}-azure-service-bus
#     spec:
#       project: default
#       sources:
#         - repoURL: '{{metadata.annotations.addons_repo_url}}'
#           targetRevision: '{{metadata.annotations.addons_repo_revision}}'
#           path: '{{metadata.annotations.addons_repo_basepath}}hooks/sleep'
#         - repoURL: '{{metadata.annotations.addons_repo_url}}'
#           targetRevision: '{{metadata.annotations.addons_repo_revision}}'
#           path: '{{metadata.annotations.addons_repo_basepath}}hooks/identity'
#           kustomize:
#             patches:
#             - target:
#                 kind: AzureClusterIdentity
#                 name: cluster-identity
#               patch: |-
#                 - op: replace
#                   path: /spec/clientID
#                   value: '{{metadata.annotations.akspe_identity_id}}'
#                 - op: replace
#                   path: /spec/tenantID
#                   value: '{{metadata.annotations.tenant_id}}'
#         - repoURL: '{{metadata.annotations.addons_repo_url}}'
#           targetRevision: '{{metadata.annotations.addons_repo_revision}}'
#           ref: values
#         - chart: '{{values.addonChart}}'
#           repoURL: '{{values.addonChartRepository}}'
#           targetRevision: '{{values.addonChartVersion}}'
#           helm:
#             releaseName: '{{values.addonChart}}'
#             ignoreMissingValueFiles: true
#             prefix: '{{metadata.annotations.cluster_name}}'
#             valueFiles:
#               - $values/{{metadata.annotations.addons_repo_basepath}}environments/default/addons/{{values.addonChart}}/values.yaml
#               - $values/{{metadata.annotations.addons_repo_basepath}}environments/{{metadata.labels.environment}}/addons/{{values.addonChart}}/values.yaml
#       destination:
#         namespace: 'azure-service-bus'
#         name: '{{name}}'
#       syncPolicy:
#         retry:
#           limit: -1 # number of failed sync attempt retries; unlimited number of attempts if less than 0
#           backoff:
#             duration: 5s # the amount to back off. Default unit is seconds, but could also be a duration (e.g. "2m", "1h")
#             factor: 2 # a factor to multiply the base duration after each failed retry
#             maxDuration: 3m # the maximum amount of time allowed for the backoff
#         automated: {}
#         syncOptions:
#           - CreateNamespace=true
#           - ServerSideApply=true
#         preserveResourcesOnDeletion: true
#       ignoreDifferences:
#       - group: apiextensions.k8s.io
#         kind: CustomResourceDefinition
#         jqPathExpressions:
#           - '.spec.conversion.webhook.clientConfig.caBundle'
