# ---
# apiVersion: argoproj.io/v1alpha1
# kind: ApplicationSet
# metadata:
#   name: addons-backstage-pg
# spec:
#   syncPolicy:
#     preserveResourcesOnDeletion: false
#   generators:
#     - clusters:
#         values:
#           addonChart: backstage-pg
#           addonChartVersion: 0.1.0
#           addonChartRepository: acraksdemo/helm
#         selector:
#           matchLabels:
#             environment: control-plane

#   template:
#     metadata:
#       name: addon-{{name}}-backstage-pg
#     spec:
#       project: default
#       sources:
#         - repoURL: '{{metadata.annotations.addons_repo_url}}'
#           targetRevision: '{{metadata.annotations.addons_repo_revision}}'
#           ref: values
#         - chart: '{{values.addonChart}}'
#           repoURL: '{{values.addonChartRepository}}'
#           targetRevision: '{{values.addonChartVersion}}'
#           helm:
#             releaseName: 'backstage-pg'
#             ignoreMissingValueFiles: true
#             valueFiles:
#               - $values/{{metadata.annotations.addons_repo_basepath}}environments/default/addons/backstage-pg/values.yaml
#       destination:
#         namespace: backstage
#         name: '{{name}}'
#       syncPolicy:
#         automated:
#           prune: false
#         syncOptions:
#           - CreateNamespace=true
#           - ServerSideApply=true
